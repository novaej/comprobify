{
  "info": {
    "name": "Comprobify API — Public",
    "description": "Comprobify developer API — self-service registration through full document lifecycle. Run in order:\n1. List Current Documents → captures agreement_version\n2. Register → captures api_key, issuer_id, tenant_id\n3. Verify Email (set verification_token manually from email)\n4. Get Current Tenant → confirm active\n5. Agreement Acceptance Status\n6. Create Invoice → captures access_key\n7. Send to SRI / Check Authorization\n8. Subscribe / Promote to Production",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "variable": [
    {
      "key": "base_url",
      "value": "https://your-api-url",
      "description": "Base URL of the API (no trailing slash)",
      "type": "string"
    },
    {
      "key": "api_key",
      "value": "",
      "description": "Bearer API key issued for the tenant (from Register or POST /v1/keys)",
      "type": "string"
    },
    {
      "key": "access_key",
      "value": "",
      "description": "49-digit SRI access key — copy from Create Invoice response",
      "type": "string"
    },
    {
      "key": "issuer_id",
      "value": "",
      "description": "Issuer ID — copy from List Issuers or Create Branch response. Used as X-Issuer-Id header on document requests.",
      "type": "string"
    },
    {
      "key": "api_key_id",
      "value": "",
      "description": "API key ID — copy from List Keys response (used for revocation)",
      "type": "string"
    },
    {
      "key": "tenant_id",
      "value": "",
      "description": "Tenant ID — copy from Create Tenant (admin) response",
      "type": "string"
    },
    {
      "key": "notification_id",
      "value": "",
      "description": "Notification ID — copy from List Notifications response (used for Mark as Read)",
      "type": "string"
    },
    {
      "key": "webhook_id",
      "value": "",
      "description": "Webhook endpoint ID — copy from Register Webhook response",
      "type": "string"
    },
    {
      "key": "subscription_id",
      "value": "",
      "description": "Subscription ID — copy from Promote to Production response (if a paid tier was requested)",
      "type": "string"
    },
    {
      "key": "payment_id",
      "value": "",
      "description": "Payment ID — copy from Promote to Production response (if a paid tier was requested)",
      "type": "string"
    },
    {
      "key": "agreement_version",
      "value": "",
      "description": "Version string of the currently published TERMS document — captured from GET /v1/agreements",
      "type": "string"
    },
    {
      "key": "admin_secret",
      "value": "",
      "description": "ADMIN_SECRET env var value — required for all /v1/admin/* requests",
      "type": "string"
    },
    {
      "key": "verification_token",
      "value": "",
      "description": "Email verification token — copy from the verification email link (?token=...)",
      "type": "string"
    }
  ],
  "item": [
    {
      "name": "Agreements",
      "item": [
        {
          "name": "List Current Documents",
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const r = pm.response.json();",
                  "pm.test('Status 200', () => pm.response.to.have.status(200));",
                  "if (r.ok && r.documents) {",
                  "  const terms = r.documents.find(d => d.documentType === 'TERMS');",
                  "  if (terms) { pm.collectionVariables.set('agreement_version', terms.version); console.log('agreement_version set to', terms.version); }",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/v1/agreements",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "agreements"
              ]
            },
            "description": "Public. Returns current version of each document type. Captures agreement_version for use in Register and Accept Agreements."
          }
        },
        {
          "name": "Get Document (TERMS)",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/v1/agreements/TERMS",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "agreements",
                "TERMS"
              ]
            },
            "description": "Returns rendered HTML. Change path segment to PRIVACY or DPA for the other documents."
          }
        },
        {
          "name": "Get Document (PRIVACY)",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/v1/agreements/PRIVACY",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "agreements",
                "PRIVACY"
              ]
            }
          }
        },
        {
          "name": "Get Document (DPA)",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/v1/agreements/DPA",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "agreements",
                "DPA"
              ]
            }
          }
        }
      ]
    },
    {
      "name": "Tiers",
      "item": [
        {
          "name": "Get Tiers",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/v1/tiers",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "tiers"
              ]
            },
            "description": "Public, no authentication required. Returns the full tier catalog (FREE/STARTER/GROWTH/BUSINESS) — quota, monthly and yearly price, overage rate, document types, branch/webhook limits, and rate limits — for a pricing page to render without hardcoding the same numbers a second time."
          }
        }
      ]
    },
    {
      "name": "Registration",
      "description": "Public registration and email verification endpoints. No authentication required.",
      "item": [
        {
          "name": "Register",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{base_url}}/v1/register",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "register"
              ]
            },
            "body": {
              "mode": "formdata",
              "formdata": [
                {
                  "key": "email",
                  "value": "your@email.com",
                  "type": "text"
                },
                {
                  "key": "ruc",
                  "value": "1712345678001",
                  "type": "text"
                },
                {
                  "key": "businessName",
                  "value": "My Company S.A.",
                  "type": "text"
                },
                {
                  "key": "tradeName",
                  "value": "",
                  "description": "Optional trade name",
                  "type": "text",
                  "disabled": true
                },
                {
                  "key": "mainAddress",
                  "value": "",
                  "description": "Optional main (fiscal) address",
                  "type": "text",
                  "disabled": true
                },
                {
                  "key": "branchCode",
                  "value": "001",
                  "type": "text"
                },
                {
                  "key": "issuePointCode",
                  "value": "001",
                  "type": "text"
                },
                {
                  "key": "emissionType",
                  "value": "1",
                  "type": "text"
                },
                {
                  "key": "requiredAccounting",
                  "value": "false",
                  "type": "text"
                },
                {
                  "key": "certPassword",
                  "value": "",
                  "type": "text"
                },
                {
                  "key": "cert",
                  "type": "file",
                  "src": ""
                },
                {
                  "key": "logo",
                  "type": "file",
                  "description": "Optional company logo for RIDE PDFs. PNG recommended (transparent background). Max 500 KB. Recommended: 600x170 px, landscape (~3.5:1 ratio).",
                  "disabled": true
                },
                {
                  "key": "specialTaxpayer",
                  "value": "",
                  "description": "Optional special taxpayer resolution number",
                  "type": "text",
                  "disabled": true
                },
                {
                  "key": "branchAddress",
                  "value": "",
                  "description": "Optional branch address",
                  "type": "text",
                  "disabled": true
                },
                {
                  "key": "documentTypes",
                  "value": "[\"01\"]",
                  "description": "Optional. Document type codes to enable (default: [\"01\"])",
                  "type": "text",
                  "disabled": true
                },
                {
                  "key": "initialSequentials",
                  "value": "[{\"documentType\": \"01\", \"sequential\": 1}]",
                  "description": "Optional. Starting sequential numbers per document type. Any type not listed defaults to 1.",
                  "type": "text",
                  "disabled": true
                },
                {
                  "key": "language",
                  "value": "es",
                  "description": "Optional. Email language: es (default) or en.",
                  "type": "text",
                  "disabled": true
                },
                {
                  "key": "verificationRedirectUrl",
                  "value": "",
                  "description": "Optional. Frontend URL for the email verification link. Token appended as ?token=<token>. https required in production.",
                  "type": "text",
                  "disabled": true
                }
              ]
            },
            "description": "Self-service registration. Creates a tenant, issuer, and sandbox API key in one call.\n\nStore the returned `apiKey` — it is shown only once. Copy it into the `api_key` collection variable.\n\nIf the email already exists and is not suspended, the endpoint revokes the current sandbox key and returns a new one (200 with `recovered: true`).\n\nOptional `verificationRedirectUrl` sets a frontend page for the email verification link."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const r = pm.response.json();",
                  "pm.test('Status 201 or 200 (recovery)', () => pm.expect(pm.response.code).to.be.oneOf([200, 201]));",
                  "pm.test('ok', () => pm.expect(r.ok).to.be.true);",
                  "if (r.apiKey) { pm.collectionVariables.set('api_key', r.apiKey); console.log('api_key saved'); }",
                  "if (r.tenant?.id) pm.collectionVariables.set('tenant_id', String(r.tenant.id));",
                  "if (r.issuer?.id) pm.collectionVariables.set('issuer_id', String(r.issuer.id));"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Verify Email",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/v1/verify-email?token={{verification_token}}",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "verify-email"
              ],
              "query": [
                {
                  "key": "token",
                  "value": "{{verification_token}}"
                }
              ]
            },
            "description": "Verify your email address using the token from the registration email. Required before promoting to production."
          }
        },
        {
          "name": "Resend Verification Email",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/resend-verification",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "resend-verification"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"your@email.com\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "description": "Resends the verification email. Generates a fresh token (invalidating the previous one) and resets the expiry.\n\nTwo rate limits apply:\n- IP-based: shared with POST /v1/register — 5 requests per hour per IP.\n- Per-account cooldown: 60 seconds between resends for the same email. Returns 429 if not elapsed.\n\nReturns the same generic message whether or not the email exists (no enumeration)."
          }
        }
      ]
    },
    {
      "name": "Tenants",
      "description": "Tenant settings and lifecycle management. Require a Bearer API key.",
      "auth": {
        "type": "bearer",
        "bearer": [
          {
            "key": "token",
            "value": "{{api_key}}",
            "type": "string"
          }
        ]
      },
      "item": [
        {
          "name": "Get Current Tenant",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/v1/tenants/me",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "me"
              ]
            },
            "description": "Resolves the tenant owning the authenticated API key (id, email, subscriptionTier, status, documentCount, documentQuota, sandbox). Useful for a third-party app that only has an API key and needs to discover its numeric tenant id."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const r = pm.response.json();",
                  "pm.test('Status 200', () => pm.response.to.have.status(200));",
                  "if (r.tenant?.id) pm.collectionVariables.set('tenant_id', String(r.tenant.id));",
                  "if (r.tenant?.agreementVersion) console.log('Last accepted agreement version:', r.tenant.agreementVersion);"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Update Preferred Language",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/tenants/language",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "language"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"language\": \"en\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "description": "Updates the preferred language for the authenticated tenant. Used for all outgoing emails. Supported values: `es` (Spanish, default), `en` (English)."
          }
        },
        {
          "name": "Promote to Production",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/tenants/promote",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "promote"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"initialSequentials\": [\n    { \"issuerId\": 1, \"documentType\": \"01\", \"sequential\": 1 }\n  ],\n  \"tier\": \"STARTER\",\n  \"billingInterval\": \"MONTHLY\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "description": "Promotes the entire tenant (all branches) from sandbox to production. This is **one-way** — cannot be reversed.\n\nAll active sandbox API keys are revoked automatically. The response includes one new production key per revoked sandbox key (same label). **Store all tokens immediately — they are shown only once.**\n\n`initialSequentials` is optional. If omitted, all active document types on all issuers start at sequential 1. Pass `issuerId` + `documentType` + `sequential` for each combination you want to pre-seed.\n\nRequires the tenant's email to be verified (status ACTIVE).\n\n**`tier` and `billingInterval` are optional.** Omit them (or pass `tier` as absent) to stay on FREE in production — promotion never waits on payment. If a paid tier is requested, this also kicks off the subscription pipeline (same as Admin's Create Subscription) and the response includes `subscription`, `payment`, and `bankTransfer` alongside `apiKeys`. The tier/quota upgrade itself only lands once that subscription is paid and its self-billed invoice is SRI-authorized — see Submit Payment Proof and Admin > Review Payment."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const r = pm.response.json();",
                  "pm.test('Status 200', () => pm.response.to.have.status(200));",
                  "if (r.apiKeys?.length) {",
                  "  pm.collectionVariables.set('api_key', r.apiKeys[0].apiKey);",
                  "  console.log('PRODUCTION API KEYS (store immediately):');",
                  "  r.apiKeys.forEach(k => console.log(' ', k.label, ':', k.apiKey));",
                  "}",
                  "if (r.subscription?.id) pm.collectionVariables.set('subscription_id', String(r.subscription.id));",
                  "if (r.payment?.id) pm.collectionVariables.set('payment_id', String(r.payment.id));"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Agreement Acceptance Status",
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const r = pm.response.json();",
                  "pm.test('Status 200', () => pm.response.to.have.status(200));",
                  "if (r.ok) {",
                  "  pm.test('needsAcceptance is boolean', () => pm.expect(typeof r.agreements.needsAcceptance).to.equal('boolean'));",
                  "  if (r.agreements.needsAcceptance) console.log('Re-acceptance needed for:', r.agreements.outdated.map(d => d.documentType));",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{api_key}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/tenants/agreements",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "agreements"
              ]
            },
            "description": "Check if any document was republished since the tenant last accepted. Use outdated[].url to show updated content."
          }
        },
        {
          "name": "Accept Agreements",
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const r = pm.response.json();",
                  "pm.test('Status 200', () => pm.response.to.have.status(200));",
                  "pm.test('ok', () => pm.expect(r.ok).to.be.true);"
                ],
                "type": "text/javascript"
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{api_key}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/tenants/agreements",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "agreements"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"termsVersion\":\"{{agreement_version}}\"}"
            },
            "description": "Record re-acceptance of all currently published documents. Use when agreements returns needsAcceptance: true."
          }
        },
        {
          "name": "List Tenant Agreements",
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const r = pm.response.json();",
                  "pm.test('Status 200', () => pm.response.to.have.status(200));",
                  "if (r.ok) {",
                  "  const pending = r.documents.filter(d => d.status === 'PENDING');",
                  "  if (pending.length) console.log('PENDING documents:', pending.map(d => d.documentType).join(', '));",
                  "  const accepted = r.documents.filter(d => d.status === 'ACCEPTED');",
                  "  console.log('ACCEPTED:', accepted.map(d => d.documentType + ' v' + d.templateVersion).join(', '));",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{api_key}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/tenants/agreements/history",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "agreements"
              ]
            },
            "description": "Lists all personalized document instances for this tenant across all versions. status: PENDING (not yet accepted) or ACCEPTED."
          }
        },
        {
          "name": "View Agreement (TERMS)",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{api_key}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/tenants/agreements/TERMS",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "agreements",
                "TERMS"
              ]
            },
            "description": "Returns the tenant's personalized Terms of Service as HTML — their businessName and RUC substituted in, with the disclaimer notice prepended. Also try /PRIVACY and /DPA. Check response headers: X-Document-Status, X-Template-Version, X-Accepted-At."
          }
        },
        {
          "name": "View Agreement (PRIVACY)",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{api_key}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/tenants/agreements/PRIVACY",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "agreements",
                "PRIVACY"
              ]
            },
            "description": "Returns the tenant's personalized Privacy Policy as HTML."
          }
        },
        {
          "name": "View Agreement (DPA)",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{api_key}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/tenants/agreements/DPA",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "agreements",
                "DPA"
              ]
            },
            "description": "DPA with the tenant's own businessName and RUC in the intro paragraph. Verify the substitution is correct before accepting."
          }
        }
      ]
    },
    {
      "name": "Payments",
      "item": [
        {
          "name": "Get Payment Proof",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{api_key}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/payments/{{payment_id}}/proof",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "payments",
                "{{payment_id}}",
                "proof"
              ]
            },
            "description": "Tenant-authenticated (your own API key). Downloads the proof-of-transfer file previously uploaded for a payment. The payment must belong to your tenant — the API enforces this via the subscription join. Returns the raw file with its original Content-Type and Content-Disposition headers. Returns 404 if no proof has been uploaded yet."
          }
        },
        {
          "name": "Submit Payment Proof",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{api_key}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/payments/{{payment_id}}/proof",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "payments",
                "{{payment_id}}",
                "proof"
              ]
            },
            "body": {
              "mode": "formdata",
              "formdata": [
                {
                  "key": "proof",
                  "type": "file",
                  "src": []
                }
              ]
            },
            "description": "Tenant-authenticated (your own API key, not admin_secret). Upload proof of the SPI bank transfer (PNG/JPEG/GIF/PDF, max 2 MB) for a payment you created via promotion, Create Subscription, Change Tier, or a renewal reminder. Moves the payment to REPORTED. Fails if the payment isn't yours, or has already been VERIFIED/REJECTED.\n\nEmails the operator (if ADMIN_NOTIFICATION_EMAIL is configured) that a proof is waiting. After this, the operator reviews the proof (Admin > Review Payment) and verifies or rejects it — you'll get an email either way."
          }
        }
      ]
    },
    {
      "name": "Subscriptions",
      "item": [
        {
          "name": "Create Subscription",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{api_key}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/subscriptions",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "subscriptions"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"tier\": \"STARTER\",\n  \"billingInterval\": \"MONTHLY\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "description": "Tenant-authenticated (your own API key). Starts a paid subscription on your own, independent of promotion — works while still in sandbox (no need to promote to production first) or any time after. Requires a verified email, same gate POST /v1/tenants/promote uses.\n\nSame manual proof/review pipeline as the rest of the subscription system: upload proof via Submit Payment Proof, the provider reviews it and links the self-billed invoice, and the tier/quota lands once that invoice is SRI-authorized.\n\nIf this subscription is already ACTIVE by the time you call Promote to Production, that call detects it automatically and skips tier selection — any tier/billingInterval you pass there is ignored, and its response surfaces this subscription instead of starting a new one.\n\nFails with 409 SUBSCRIPTION_ALREADY_IN_FLIGHT if you already have one in progress, or 403 EMAIL_VERIFICATION_REQUIRED if your email isn't verified yet."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const r = pm.response.json();",
                  "pm.test('Status 201', () => pm.response.to.have.status(201));",
                  "if (r.ok) {",
                  "  if (r.subscription?.id) { pm.collectionVariables.set('subscription_id', String(r.subscription.id)); console.log('subscription_id:', r.subscription.id); }",
                  "  if (r.payment?.id) { pm.collectionVariables.set('payment_id', String(r.payment.id)); console.log('payment_id:', r.payment.id); }",
                  "  if (r.bankTransfer) console.log('Bank transfer info:', JSON.stringify(r.bankTransfer));",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Get My Subscriptions",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{api_key}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/subscriptions/me",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "subscriptions",
                "me"
              ]
            },
            "description": "Tenant-authenticated (your own API key). Returns your full subscription history, newest first, each with its payments nested (status, amount, rejection_reason if rejected). The proof file itself is never included — only filename/mime type.\n\nThis is how you find out a paid-tier upgrade landed — there's no notification; `subscriptionTier`/`documentQuota` on `GET /v1/tenants/me` update automatically once it does, but this endpoint shows the in-between states (and the `rejection_reason` if a proof was rejected, so you know what to fix before re-uploading via Submit Payment Proof)."
          }
        },
        {
          "name": "Change Tier (Upgrade/Downgrade)",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{api_key}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/subscriptions/change-tier",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "subscriptions",
                "change-tier"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"tier\": \"GROWTH\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "description": "Tenant-authenticated (your own API key). Changes the tier on your existing ACTIVE subscription — requires having already promoted with a paid tier (see Promote to Production / Admin > Create Subscription). No payment gateway exists, so this rides the same manual proof/review pipeline as the initial subscription rather than charging anything automatically.\n\n**Upgrade** (target tier's price higher than your current one): takes effect immediately once paid. The response includes a prorated `payment` (the price difference scaled by the remaining time in your current billing period) and `bankTransfer` instructions — upload proof via the same Submit Payment Proof endpoint used for your initial subscription. If the prorated amount rounds to $0 (almost no time left in the period), it's applied immediately with no payment step at all.\n\n**Downgrade** (target tier's price lower): scheduled, not immediate, and needs no payment — you already paid for the current period at the higher tier. Your tier/quota stay exactly as they are until `current_period_end`, at which point it's applied automatically by the provider's scheduled job.\n\nTo cancel entirely and return to FREE, use `DELETE /v1/subscriptions` instead.\n\nFails with 409 `NO_ACTIVE_SUBSCRIPTION` if you have no ACTIVE subscription, 400 `TIER_CHANGE_NO_OP` if `tier` matches your current one, or 409 `TIER_CHANGE_ALREADY_PENDING` if a change is already in flight (only one at a time).\n\nPoll `GET /v1/subscriptions/me` to see status, and `GET /v1/tenants/me` once it lands."
          }
        },
        {
          "name": "Cancel Subscription",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{api_key}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/subscriptions",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "subscriptions"
              ]
            },
            "description": "Tenant-authenticated (your own API key). Schedules a cancellation at the end of the current billing period — no refund is issued. The subscription keeps running at the current tier until `current_period_end`; the provider's daily scheduled job then drops the tenant to FREE and closes the subscription.\n\nSets `pending_tier = 'FREE'` on the subscription (same mechanism as a scheduled downgrade). No renewal reminder will be issued for a pending-cancellation subscription.\n\nFails with 409 `NO_ACTIVE_SUBSCRIPTION` if you have no ACTIVE subscription, or 409 `CANCELLATION_ALREADY_PENDING` if a cancellation is already scheduled. Also fails with 409 `TIER_CHANGE_ALREADY_PENDING` if a paid-tier downgrade or upgrade is already in flight.\n\nPoll `GET /v1/subscriptions/me` to confirm status flipped to CANCELLED, or `GET /v1/tenants/me` to confirm the tier dropped to FREE."
          }
        }
      ]
    },
    {
      "name": "API Keys",
      "description": "Tenant-facing API key management. Mint named keys for each integration, list them, and revoke leaked or unused ones. Require a Bearer API key.",
      "auth": {
        "type": "bearer",
        "bearer": [
          {
            "key": "token",
            "value": "{{api_key}}",
            "type": "string"
          }
        ]
      },
      "item": [
        {
          "name": "List Keys",
          "request": {
            "method": "GET",
            "url": {
              "raw": "{{base_url}}/v1/keys",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "keys"
              ]
            },
            "description": "Returns every active key for the tenant. The plaintext token is **never** returned — only labels, environments, ids, and creation dates.\n\nCopy a key's `id` into the `api_key_id` variable to use it with Revoke Key."
          }
        },
        {
          "name": "Mint Key",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/keys",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "keys"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"label\": \"erp-integration\",\n  \"environment\": \"sandbox\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "description": "Creates a new tenant-scoped API key. The plaintext token is shown **once** in the response — store it immediately.\n\n`label` (optional, max 100 chars) — a human-readable name for the integration, e.g. `frontend-prod`, `erp`, `mobile-app`.\n\n`environment` — `\"sandbox\"` (default) or `\"production\"`. Production keys can only be minted after the tenant has been promoted to production.\n\nWhen used on a document request, the key's environment must match the targeted issuer's environment — otherwise the request is rejected with 401."
          }
        },
        {
          "name": "Revoke Key",
          "request": {
            "method": "DELETE",
            "url": {
              "raw": "{{base_url}}/v1/keys/{{api_key_id}}",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "keys",
                "{{api_key_id}}"
              ]
            },
            "description": "Marks the key as inactive. The key stops working immediately and cannot be used to authenticate any future request.\n\nYou cannot revoke the key you are currently using to make this request — use a different key, or contact admin support."
          }
        }
      ]
    },
    {
      "name": "Issuers",
      "description": "Issuer (branch / issue point) management. Require a Bearer API key.",
      "auth": {
        "type": "bearer",
        "bearer": [
          {
            "key": "token",
            "value": "{{api_key}}",
            "type": "string"
          }
        ]
      },
      "item": [
        {
          "name": "List Issuers",
          "request": {
            "method": "GET",
            "url": {
              "raw": "{{base_url}}/v1/issuers",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "issuers"
              ]
            },
            "description": "Returns all active issuers (branches / issue points) belonging to the authenticated tenant.\n\nCopy an issuer's `id` into the `issuer_id` collection variable to use it as `X-Issuer-Id` on document requests."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const r = pm.response.json();",
                  "pm.test('Status 200', () => pm.response.to.have.status(200));",
                  "if (r.ok && r.issuers?.length) { pm.collectionVariables.set('issuer_id', String(r.issuers[0].id)); console.log('issuer_id set to', r.issuers[0].id); }"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Get Issuer",
          "request": {
            "method": "GET",
            "url": {
              "raw": "{{base_url}}/v1/issuers/{{issuer_id}}",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "issuers",
                "{{issuer_id}}"
              ]
            },
            "description": "Returns profile information for a single issuer owned by the authenticated tenant: RUC, business name, trade name, branch/issue point codes, and certificate expiry/fingerprint."
          }
        },
        {
          "name": "Upload Issuer Logo",
          "request": {
            "method": "PATCH",
            "header": [],
            "body": {
              "mode": "formdata",
              "formdata": [
                {
                  "key": "logo",
                  "type": "file",
                  "description": "Company logo (PNG, JPEG, or GIF; max 500 KB)"
                }
              ]
            },
            "url": {
              "raw": "{{base_url}}/v1/issuers/{{issuer_id}}/logo",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "issuers",
                "{{issuer_id}}",
                "logo"
              ]
            },
            "description": "Uploads or replaces the company logo for an issuer. The logo is stored in the database and rendered in the top-left corner of every RIDE PDF generated for that issuer, including PDFs sent as email attachments. Accepted formats: PNG, JPEG, GIF. Max size: 500 KB."
          }
        },
        {
          "name": "Renew Certificate",
          "request": {
            "method": "PATCH",
            "header": [],
            "body": {
              "mode": "formdata",
              "formdata": [
                {
                  "key": "cert",
                  "type": "file",
                  "src": "",
                  "description": "P12/PFX certificate file from an authorized Ecuadorian CA (BANCO CENTRAL or SECURITY DATA)"
                },
                {
                  "key": "certPassword",
                  "value": "",
                  "type": "text",
                  "description": "Optional. Password protecting the P12 file, if any.",
                  "disabled": true
                }
              ]
            },
            "url": {
              "raw": "{{base_url}}/v1/issuers/{{issuer_id}}/certificate",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "issuers",
                "{{issuer_id}}",
                "certificate"
              ]
            },
            "description": "Replaces the P12 certificate (private key + X.509 cert) for an issuer — e.g. when it has expired. Does not affect already-signed documents, since each signed invoice embeds its own copy of the certificate inside the XML signature; only future signing operations use the new certificate."
          }
        },
        {
          "name": "Create Branch",
          "request": {
            "method": "POST",
            "url": {
              "raw": "{{base_url}}/v1/issuers",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "issuers"
              ]
            },
            "body": {
              "mode": "formdata",
              "formdata": [
                {
                  "key": "branchCode",
                  "value": "002",
                  "type": "text"
                },
                {
                  "key": "issuePointCode",
                  "value": "001",
                  "type": "text"
                },
                {
                  "key": "branchAddress",
                  "value": "Av. 6 de Diciembre 123",
                  "type": "text",
                  "disabled": true
                },
                {
                  "key": "documentTypes",
                  "value": "[\"01\"]",
                  "description": "Optional. Document type codes to enable (default: [\"01\"])",
                  "type": "text",
                  "disabled": true
                },
                {
                  "key": "sourceIssuerId",
                  "value": "{{issuer_id}}",
                  "description": "Optional. Numeric id of the issuer to inherit cert/profile from. Defaults to the tenant's first existing issuer. Ignored if a cert file is uploaded.",
                  "type": "text",
                  "disabled": true
                },
                {
                  "key": "cert",
                  "type": "file",
                  "src": "",
                  "disabled": true,
                  "description": "Optional — leave blank to reuse the calling issuer certificate"
                },
                {
                  "key": "certPassword",
                  "value": "",
                  "type": "text",
                  "disabled": true
                }
              ]
            },
            "description": "Creates a new branch or issue point for the tenant. Inherits RUC, business name, and certificate from an existing issuer of the tenant.\n\n**No new API key is minted** — your existing tenant key already covers every branch via the `X-Issuer-Id` header. New branches inherit the tenant's current environment (sandbox or production).\n\nThe cert file and certPassword fields are optional — only provide them if the new branch needs a different certificate.\n\nTier limits apply: FREE allows 1 branch / 1 issue point, STARTER 3 / 2, GROWTH 10 / 5, BUSINESS unlimited."
          }
        },
        {
          "name": "List Document Types",
          "request": {
            "method": "GET",
            "url": {
              "raw": "{{base_url}}/v1/issuers/{{issuer_id}}/document-types",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "issuers",
                "{{issuer_id}}",
                "document-types"
              ]
            },
            "description": "Returns the active document types enabled for the specified issuer."
          }
        },
        {
          "name": "Add Document Type",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/issuers/{{issuer_id}}/document-types",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "issuers",
                "{{issuer_id}}",
                "document-types"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"documentType\": \"01\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "description": "Enables a document type for the specified issuer. If the type was previously removed, it is reactivated. Returns the full updated list of active document types."
          }
        },
        {
          "name": "Remove Document Type",
          "request": {
            "method": "DELETE",
            "url": {
              "raw": "{{base_url}}/v1/issuers/{{issuer_id}}/document-types/01",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "issuers",
                "{{issuer_id}}",
                "document-types",
                "01"
              ]
            },
            "description": "Disables a document type for the specified issuer. The last active type cannot be removed."
          }
        },
        {
          "name": "Update Issuer",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/issuers/{{issuer_id}}",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "issuers",
                "{{issuer_id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"tradeName\": \"ACME Express\",\n  \"branchAddress\": \"Av. Amazonas 456\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "description": "Edits the trade name and/or branch address for an existing issuer. At least one field is required. businessName, mainAddress, and ruc cannot be edited here."
          }
        },
        {
          "name": "Remove Issuer",
          "request": {
            "method": "DELETE",
            "url": {
              "raw": "{{base_url}}/v1/issuers/{{issuer_id}}",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "issuers",
                "{{issuer_id}}"
              ]
            },
            "description": "Soft-deletes an issuer (active = false). Rejected with LAST_ISSUER_CANNOT_BE_REMOVED if it's the tenant's only active issuer, or ISSUER_HAS_DOCUMENTS if it has ever issued a document."
          }
        },
        {
          "name": "Get Sequentials",
          "request": {
            "method": "GET",
            "url": {
              "raw": "{{base_url}}/v1/issuers/{{issuer_id}}/sequentials",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "issuers",
                "{{issuer_id}}",
                "sequentials"
              ]
            },
            "description": "Returns the current and next sequential number per active document type, broken out by sandbox/production environment."
          }
        },
        {
          "name": "Set Next Sequential",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/issuers/{{issuer_id}}/sequentials/01",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "issuers",
                "{{issuer_id}}",
                "sequentials",
                "01"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"environment\": \"production\",\n  \"nextSequential\": 200\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "description": "Manually sets the next sequential number for one document type in one environment. Locks the counter row with SELECT ... FOR UPDATE so it cannot race a concurrent POST /v1/documents call. nextSequential must exceed the current counter value."
          }
        },
        {
          "name": "Activate Issuer",
          "request": {
            "method": "PATCH",
            "url": {
              "raw": "{{base_url}}/v1/issuers/{{issuer_id}}/activate",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "issuers",
                "{{issuer_id}}",
                "activate"
              ]
            },
            "description": "Reactivates a soft-deleted issuer. Re-checks the tenant's plan branch/issue-point limits the same way Create Branch does, so deactivate-then-reactivate cannot be used to exceed plan caps."
          }
        }
      ]
    },
    {
      "name": "Documents",
      "description": "Document lifecycle endpoints. All requests require a Bearer API key and the `X-Issuer-Id` header identifying the issuing branch.",
      "auth": {
        "type": "bearer",
        "bearer": [
          {
            "key": "token",
            "value": "{{api_key}}",
            "type": "string"
          }
        ]
      },
      "item": [
        {
          "name": "Create Invoice",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "X-Issuer-Id",
                "value": "{{issuer_id}}",
                "description": "Numeric id of the issuing branch (from GET /v1/issuers)"
              },
              {
                "key": "Idempotency-Key",
                "value": "{{$guid}}",
                "description": "Optional — unique per intended invoice. Reuse on retries to avoid duplicates."
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/documents",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "documents"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"documentType\": \"01\",\n  \"issueDate\": \"05/04/2026\",\n  \"buyer\": {\n    \"idType\": \"05\",\n    \"id\": \"1234567890\",\n    \"name\": \"John Doe\",\n    \"email\": \"john@example.com\",\n    \"address\": \"Av. Amazonas 123\"\n  },\n  \"items\": [\n    {\n      \"mainCode\": \"PROD-001\",\n      \"auxiliaryCode\": \"AUX-001\",\n      \"description\": \"Web development service\",\n      \"quantity\": \"1.00\",\n      \"unitPrice\": \"100.00\",\n      \"discount\": \"0.00\",\n      \"taxes\": [\n        {\n          \"code\": \"2\",\n          \"rateCode\": \"2\",\n          \"rate\": \"15.00\",\n          \"taxableBase\": \"100.00\",\n          \"taxAmount\": \"15.00\"\n        }\n      ]\n    }\n  ],\n  \"payments\": [\n    {\n      \"method\": \"01\",\n      \"total\": \"115.00\",\n      \"term\": 30,\n      \"termUnit\": \"dias\"\n    }\n  ],\n  \"additionalInfo\": [\n    { \"name\": \"Contract\", \"value\": \"CTR-2026-001\" }\n  ]\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "description": "Creates, XSD-validates, and signs a new electronic invoice. Returns the document with status `SIGNED`.\n\nAfter a successful response, copy the `accessKey` value into the `access_key` collection variable.\n\nThe `X-Issuer-Id` header identifies which branch and certificate to use. Get the id from `GET /v1/issuers`.\n\nThe `Idempotency-Key` header makes creation idempotent — generate it once per intended invoice and reuse on retries."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const r = pm.response.json();",
                  "pm.test('Status 201', () => pm.response.to.have.status(201));",
                  "if (r.document?.accessKey) { pm.collectionVariables.set('access_key', r.document.accessKey); console.log('access_key saved'); }"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Create Credit Note",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "X-Issuer-Id",
                "value": "{{issuer_id}}",
                "description": "Numeric id of the issuing branch (from GET /v1/issuers). Must have document type 04 enabled."
              },
              {
                "key": "Idempotency-Key",
                "value": "{{$guid}}",
                "description": "Optional — unique per intended credit note. Reuse on retries to avoid duplicates."
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/documents",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "documents"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"documentType\": \"04\",\n  \"issueDate\": \"05/04/2026\",\n  \"buyer\": {\n    \"idType\": \"05\",\n    \"id\": \"1234567890\",\n    \"name\": \"John Doe\",\n    \"email\": \"john@example.com\",\n    \"address\": \"Av. Amazonas 123\"\n  },\n  \"originalDocument\": {\n    \"documentType\": \"01\",\n    \"number\": \"001-001-000000027\",\n    \"issueDate\": \"03/04/2026\"\n  },\n  \"motivo\": \"Devolucion de mercaderia por defecto de fabricacion\",\n  \"items\": [\n    {\n      \"mainCode\": \"PROD-001\",\n      \"auxiliaryCode\": \"AUX-001\",\n      \"description\": \"Web development service\",\n      \"quantity\": \"1.00\",\n      \"unitPrice\": \"100.00\",\n      \"discount\": \"0.00\",\n      \"taxes\": [\n        {\n          \"code\": \"2\",\n          \"rateCode\": \"2\",\n          \"rate\": \"15.00\",\n          \"taxableBase\": \"100.00\",\n          \"taxAmount\": \"15.00\"\n        }\n      ]\n    }\n  ],\n  \"additionalInfo\": [\n    { \"name\": \"Contract\", \"value\": \"CTR-2026-001\" }\n  ]\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "description": "Creates, XSD-validates, and signs a new credit note referencing a previously authorized invoice. Returns the document with status `SIGNED`.\n\nUnlike Create Invoice, this body has no `payments` block and instead requires `originalDocument` (the invoice being credited — its document type, number, and issue date) plus a `motivo` (reason for the credit note).\n\nThe `X-Issuer-Id` header identifies which branch and certificate to use — the issuer must have document type `04` enabled first (see Add Document Type).\n\nThe `Idempotency-Key` header makes creation idempotent — generate it once per intended credit note and reuse on retries."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const r = pm.response.json();",
                  "pm.test('Status 201', () => pm.response.to.have.status(201));",
                  "if (r.document?.accessKey) pm.collectionVariables.set('access_key', r.document.accessKey);"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "List Documents",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Issuer-Id",
                "value": "{{issuer_id}}",
                "description": "Numeric id of the issuing branch (from GET /v1/issuers)"
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/documents?page=1&limit=10",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "documents"
              ],
              "query": [
                {
                  "key": "page",
                  "value": "1",
                  "description": "Page number (default: 1)",
                  "disabled": true
                },
                {
                  "key": "limit",
                  "value": "10",
                  "description": "Results per page, 1-100 (default: 10)",
                  "disabled": true
                },
                {
                  "key": "status",
                  "value": "AUTHORIZED",
                  "description": "Filter by status: SIGNED, RECEIVED, RETURNED, AUTHORIZED, NOT_AUTHORIZED",
                  "disabled": true
                },
                {
                  "key": "documentType",
                  "value": "01",
                  "description": "Filter by document type code (e.g., 01)",
                  "disabled": true
                },
                {
                  "key": "from",
                  "value": "01/01/2026",
                  "description": "Filter by issue date >= DD/MM/YYYY",
                  "disabled": true
                },
                {
                  "key": "to",
                  "value": "31/12/2026",
                  "description": "Filter by issue date <= DD/MM/YYYY",
                  "disabled": true
                },
                {
                  "key": "sequential",
                  "value": "000000001",
                  "description": "Filter by sequential, contains match against the zero-padded 9-digit value (case-insensitive)",
                  "disabled": true
                },
                {
                  "key": "buyerName",
                  "value": "ACME",
                  "description": "Filter by buyer name, contains match (case-insensitive)",
                  "disabled": true
                },
                {
                  "key": "sortBy",
                  "value": "issueDate",
                  "description": "Sort by: sequential, buyerName, issueDate, status. Defaults to created_at DESC when omitted.",
                  "disabled": true
                },
                {
                  "key": "sortDir",
                  "value": "desc",
                  "description": "Sort direction: asc, desc. Defaults to desc when sortBy is given without sortDir.",
                  "disabled": true
                }
              ]
            },
            "description": "Lists documents for the specified issuer with optional filtering, sorting, and pagination. Defaults to creation date (newest first) when no sortBy is given."
          }
        },
        {
          "name": "Document Stats",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Issuer-Id",
                "value": "{{issuer_id}}",
                "description": "Numeric id of the issuing branch (from GET /v1/issuers)"
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/documents/stats",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "documents",
                "stats"
              ]
            },
            "description": "Returns a per-type document breakdown for the current month plus an all-time needs-attention count, for dashboard summaries."
          }
        },
        {
          "name": "Get Document",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Issuer-Id",
                "value": "{{issuer_id}}",
                "description": "Numeric id of the issuing branch (from GET /v1/issuers)"
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/documents/{{access_key}}",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "documents",
                "{{access_key}}"
              ]
            },
            "description": "Retrieves document metadata by access key. Does not call the SRI — use Check Authorization for status updates."
          }
        },
        {
          "name": "Send to SRI",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Issuer-Id",
                "value": "{{issuer_id}}",
                "description": "Numeric id of the issuing branch (from GET /v1/issuers)"
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/documents/{{access_key}}/send",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "documents",
                "{{access_key}}",
                "send"
              ]
            },
            "description": "Submits the signed XML to the SRI reception service. Document moves to `RECEIVED` or `RETURNED`."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const r = pm.response.json();",
                  "pm.test('Status 200', () => pm.response.to.have.status(200));",
                  "pm.test('Status RECEIVED or RETURNED', () => pm.expect(['RECEIVED','RETURNED']).to.include(r.document?.status));"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Check Authorization",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Issuer-Id",
                "value": "{{issuer_id}}",
                "description": "Numeric id of the issuing branch (from GET /v1/issuers)"
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/documents/{{access_key}}/authorize",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "documents",
                "{{access_key}}",
                "authorize"
              ]
            },
            "description": "Queries the SRI authorization service. Document moves to `AUTHORIZED` or `NOT_AUTHORIZED`. On authorization, an email with the RIDE PDF and XML is sent to the buyer."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const r = pm.response.json();",
                  "pm.test('Status 200', () => pm.response.to.have.status(200));",
                  "if (r.document?.status) console.log('Document status:', r.document.status);"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "Rebuild Invoice",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "X-Issuer-Id",
                "value": "{{issuer_id}}",
                "description": "Numeric id of the issuing branch (from GET /v1/issuers)"
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/documents/{{access_key}}/rebuild",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "documents",
                "{{access_key}}",
                "rebuild"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"documentType\": \"01\",\n  \"buyer\": {\n    \"idType\": \"05\",\n    \"id\": \"1234567890\",\n    \"name\": \"John Doe\",\n    \"email\": \"john@example.com\",\n    \"address\": \"Av. Amazonas 123\"\n  },\n  \"items\": [\n    {\n      \"mainCode\": \"PROD-001\",\n      \"description\": \"Web development service (corrected)\",\n      \"quantity\": \"1.00\",\n      \"unitPrice\": \"100.00\",\n      \"discount\": \"0.00\",\n      \"taxes\": [\n        {\n          \"code\": \"2\",\n          \"rateCode\": \"2\",\n          \"rate\": \"15.00\",\n          \"taxableBase\": \"100.00\",\n          \"taxAmount\": \"15.00\"\n        }\n      ]\n    }\n  ],\n  \"payments\": [\n    {\n      \"method\": \"01\",\n      \"total\": \"115.00\"\n    }\n  ]\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "description": "Corrects and re-signs a document in `RETURNED` or `NOT_AUTHORIZED` status. Keeps the original `accessKey`, `sequential`, and `issueDate`. Send again after rebuilding."
          }
        },
        {
          "name": "Get RIDE PDF",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Issuer-Id",
                "value": "{{issuer_id}}",
                "description": "Numeric id of the issuing branch (from GET /v1/issuers)"
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/documents/{{access_key}}/ride",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "documents",
                "{{access_key}}",
                "ride"
              ]
            },
            "description": "Downloads the RIDE PDF for an `AUTHORIZED` document. Returns `application/pdf`."
          }
        },
        {
          "name": "Get XML",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Issuer-Id",
                "value": "{{issuer_id}}",
                "description": "Numeric id of the issuing branch (from GET /v1/issuers)"
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/documents/{{access_key}}/xml",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "documents",
                "{{access_key}}",
                "xml"
              ]
            },
            "description": "Downloads the authorization XML for an `AUTHORIZED` document, or the signed XML for earlier statuses. Returns `application/xml`."
          }
        },
        {
          "name": "Get Events",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Issuer-Id",
                "value": "{{issuer_id}}",
                "description": "Numeric id of the issuing branch (from GET /v1/issuers)"
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/documents/{{access_key}}/events",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "documents",
                "{{access_key}}",
                "events"
              ]
            },
            "description": "Returns the full audit trail for the document — every lifecycle transition, SRI call, and email event."
          }
        },
        {
          "name": "Get Credit Notes",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Issuer-Id",
                "value": "{{issuer_id}}",
                "description": "Numeric id of the issuing branch (from GET /v1/issuers)"
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/documents/{{access_key}}/credit-notes",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "documents",
                "{{access_key}}",
                "credit-notes"
              ]
            },
            "description": "Returns the sum of all AUTHORIZED credit notes already issued against this document (use the original invoice's access key, not a credit note's own key), plus the remaining balance. Only AUTHORIZED credit notes count — pending or rejected ones are excluded."
          }
        },
        {
          "name": "Retry Single Email",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Issuer-Id",
                "value": "{{issuer_id}}",
                "description": "Numeric id of the issuing branch (from GET /v1/issuers)"
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/documents/{{access_key}}/email-retry",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "documents",
                "{{access_key}}",
                "email-retry"
              ],
              "query": [
                {
                  "key": "force",
                  "value": "true",
                  "description": "Set to true to resend an email that was already successfully delivered",
                  "disabled": true
                }
              ]
            },
            "description": "Retries the authorization email for a single document. Document must be `AUTHORIZED` with `email_status` of `PENDING` or `FAILED`. Add `?force=true` to resend an already-delivered email."
          }
        },
        {
          "name": "Retry Emails (batch)",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Issuer-Id",
                "value": "{{issuer_id}}",
                "description": "Numeric id of the issuing branch (from GET /v1/issuers)"
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/documents/email-retry",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "documents",
                "email-retry"
              ]
            },
            "description": "Retries all `PENDING` and `FAILED` authorization emails across all documents for the specified issuer. Maximum 100 per call."
          }
        }
      ]
    },
    {
      "name": "Catalogs",
      "description": "Reference data for SRI-defined codes. Use these to build valid invoice requests. Require a Bearer API key.",
      "auth": {
        "type": "bearer",
        "bearer": [
          {
            "key": "token",
            "value": "{{api_key}}",
            "type": "string"
          }
        ]
      },
      "item": [
        {
          "name": "ID Types",
          "request": {
            "method": "GET",
            "url": {
              "raw": "{{base_url}}/v1/catalogs/id-types",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "catalogs",
                "id-types"
              ]
            },
            "description": "Returns buyer identification types (e.g. 04 = RUC, 05 = Cédula, 06 = Pasaporte). Use the `code` as `buyer.idType` on invoice creation."
          }
        },
        {
          "name": "Payment Methods",
          "request": {
            "method": "GET",
            "url": {
              "raw": "{{base_url}}/v1/catalogs/payment-methods",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "catalogs",
                "payment-methods"
              ]
            },
            "description": "Returns SRI payment method codes. Use the `code` as `payments[].method` on invoice creation."
          }
        },
        {
          "name": "Term Units",
          "request": {
            "method": "GET",
            "url": {
              "raw": "{{base_url}}/v1/catalogs/term-units",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "catalogs",
                "term-units"
              ]
            },
            "description": "Returns accepted SRI payment term units (e.g. dias, meses). Use the `code` as `payments[].termUnit` (with `payments[].term`) when sending invoices a plazos."
          }
        },
        {
          "name": "Tax Types",
          "request": {
            "method": "GET",
            "url": {
              "raw": "{{base_url}}/v1/catalogs/tax-types",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "catalogs",
                "tax-types"
              ]
            },
            "description": "Returns SRI tax type codes (e.g. 2 = IVA, 3 = ICE, 5 = IRBPNR). Use the `code` as `taxes[].code` on invoice items."
          }
        },
        {
          "name": "Tax Rates",
          "request": {
            "method": "GET",
            "url": {
              "raw": "{{base_url}}/v1/catalogs/tax-rates",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "catalogs",
                "tax-rates"
              ]
            },
            "description": "Returns tax rate codes grouped by tax type. Use `taxCode` as `taxes[].code` and `rateCode` as `taxes[].rateCode` on invoice items."
          }
        }
      ]
    },
    {
      "name": "Notifications",
      "description": "Tenant-level alerts. Both read and unread are returned; use `readAt` to decide what to show. Use `?sinceId=<id>` for efficient catch-up polling — pass the highest id seen on the last request to receive only new notifications.\n\nAll requests require a Bearer API key.",
      "auth": {
        "type": "bearer",
        "bearer": [
          {
            "key": "token",
            "value": "{{api_key}}",
            "type": "string"
          }
        ]
      },
      "item": [
        {
          "name": "List Notifications",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Issuer-Id",
                "value": "{{issuer_id}}",
                "description": "Optional — filter to a specific issuer's notifications plus tenant-level ones. Omit for all-tenant view.",
                "disabled": true
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/notifications",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "notifications"
              ],
              "query": [
                {
                  "key": "sinceId",
                  "value": "0",
                  "description": "Optional — return only notifications with id > sinceId. Store the highest id seen and pass it on the next poll to catch up efficiently.",
                  "disabled": true
                }
              ]
            },
            "description": "Returns active (unexpired) notifications for the tenant, newest first. Both read and unread are included.\n\n**Polling pattern:** call on a schedule (e.g. every 60 s). Use `?sinceId=<id>` to retrieve only notifications created after your last poll — store the highest `id` you've seen and pass it on the next request.\n\nOptional `X-Issuer-Id` header restricts results to a specific issuer's notifications plus tenant-level ones (`issuerId: null`). Omit it for an all-tenant view."
          }
        },
        {
          "name": "Mark as Read",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{base_url}}/v1/notifications/{{notification_id}}/read",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "{{notification_id}}",
                "read"
              ]
            },
            "description": "Marks a notification as read at the tenant level. Call this only when every user with access to the notification has acknowledged it on the frontend side. After this call, the notification no longer contributes to `unreadCount` on future polls.\n\nCopy a notification `id` from the List Notifications response into the `notification_id` variable."
          }
        },
        {
          "name": "Get Preferences",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/v1/notifications/preferences",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "preferences"
              ]
            },
            "description": "Returns the full notification preference list for the tenant — one entry per notification type. Types the tenant has never explicitly configured default to `enabled: true` (opt-out model)."
          }
        },
        {
          "name": "Update Preferences",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/notifications/preferences",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "preferences"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "[\n  { \"type\": \"DOCUMENT_AUTHORIZED\", \"enabled\": true },\n  { \"type\": \"CERT_EXPIRING\",        \"enabled\": true },\n  { \"type\": \"CERT_EXPIRED\",         \"enabled\": true }\n]",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "description": "Bulk-upsert notification preferences for the tenant. Send only the types you want to change — unmentioned types are unchanged.\n\nWhen `enabled` is `false` for a type, no new notifications of that type are created. Existing unread notifications remain and can still be marked as read.\n\nValid types: `DOCUMENT_AUTHORIZED`, `CERT_EXPIRING`, `CERT_EXPIRED`, `SRI_SUBMISSION_FAILED`, `EMAIL_DELIVERY_FAILED`, `QUOTA_WARNING`."
          }
        }
      ]
    },
    {
      "name": "Webhooks",
      "description": "Register HTTPS callback URLs to receive event notifications in near-real time. When a notification is created or updated, the API immediately POSTs a signed payload to every subscribed active endpoint.\n\n**Secret handling:** the signing secret is returned only at registration. Store it immediately — it cannot be retrieved later. If lost, deregister and re-register the endpoint.\n\n**Tier limits:** FREE: 1 endpoint, STARTER: 2, GROWTH: 5, BUSINESS: 10.\n\nAll requests require a Bearer API key.",
      "auth": {
        "type": "bearer",
        "bearer": [
          {
            "key": "token",
            "value": "{{api_key}}",
            "type": "string"
          }
        ]
      },
      "item": [
        {
          "name": "Register Webhook",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/webhooks",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "webhooks"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"url\": \"https://app.example.com/v1/comprobify/events\",\n  \"eventTypes\": [\"DOCUMENT_AUTHORIZED\", \"CERT_EXPIRING\", \"CERT_EXPIRED\"]\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "description": "Registers a new webhook endpoint and returns the signing `secret` — **shown only once**. Store it immediately; it is used to verify `X-Comprobify-Signature` on incoming webhook requests.\n\n`url` must be a valid HTTPS URL.\n\n`eventTypes` is optional. Omit it or pass `[]` to subscribe to **all** event types. Valid values: `DOCUMENT_AUTHORIZED`, `CERT_EXPIRING`, `CERT_EXPIRED`, `SRI_SUBMISSION_FAILED`, `EMAIL_DELIVERY_FAILED`, `QUOTA_WARNING`.\n\nCopy the returned `id` into the `webhook_id` collection variable."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const r = pm.response.json();",
                  "pm.test('Status 201', () => pm.response.to.have.status(201));",
                  "if (r.endpoint?.id) pm.collectionVariables.set('webhook_id', String(r.endpoint.id));",
                  "if (r.endpoint?.secret) console.log('Webhook secret (shown once):', r.endpoint.secret);"
                ],
                "type": "text/javascript"
              }
            }
          ]
        },
        {
          "name": "List Webhooks",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/v1/webhooks",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "webhooks"
              ]
            },
            "description": "Returns all active webhook endpoints for the tenant. Signing secrets are never included in list responses."
          }
        },
        {
          "name": "Update Webhook",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/v1/webhooks/{{webhook_id}}",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "webhooks",
                "{{webhook_id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"url\": \"https://app.example.com/v1/comprobify/events-v2\",\n  \"eventTypes\": [\"DOCUMENT_AUTHORIZED\"],\n  \"active\": true\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "description": "Updates a webhook endpoint's URL, event subscriptions, or active flag. All fields are optional — send only what you want to change.\n\nCopy an endpoint `id` from the List Webhooks response into the `webhook_id` variable."
          }
        },
        {
          "name": "Deregister Webhook",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{base_url}}/v1/webhooks/{{webhook_id}}",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "v1",
                "webhooks",
                "{{webhook_id}}"
              ]
            },
            "description": "Soft-deletes a webhook endpoint (`active = false`). The endpoint stops receiving deliveries immediately. Past delivery records are preserved for audit purposes.\n\nCopy an endpoint `id` from the List Webhooks response into the `webhook_id` variable."
          }
        }
      ]
    },
    {
      "name": "Health",
      "description": "Monitoring endpoint. No authentication required.",
      "item": [
        {
          "name": "Health Check",
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "GET",
            "url": {
              "raw": "{{base_url}}/health",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "health"
              ]
            },
            "description": "Returns `{ status: \"ok\", uptime }` (200) when the database is reachable, or `{ status: \"error\", uptime }` (503) when it is not. Use for load balancer health checks and container liveness probes."
          }
        }
      ]
    }
  ]
}